syntax = "proto3";

package user;

option csharp_namespace = "SurveyService.User";

service UserService {
  // Define RPC methods here in the future
  rpc GetStatus (GetStatusRequest) returns (GetStatusResponse);
}

message GetStatusRequest {}
message GetStatusResponse {
  string status = 1;
}
